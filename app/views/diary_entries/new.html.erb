<div class="max-w-md mx-auto p-4">
  <h1 class="text-xl font-bold mb-4">新しい日記を書く</h1>

  <%= form_with model: @entry, local: true, class: "space-y-4" do |f| %>
    
    <%# 日付 %>
    <div>
      <%= f.label :walked_on, "日付", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.date_field :walked_on, class: "w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      <% if @entry.errors[:walked_on].any? %>
        <p class="text-red-500 text-sm mt-1"><%= @entry.errors[:walked_on].first %></p>
      <% end %>
    </div>

    <%# 気分 %>
    <div>
      <%= f.label :mood, "気分", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.select :mood, [["😀 いい感じ", "good"], ["😐 ふつう", "normal"], ["😞 つかれた", "tired"], ["🤩 わくわく", "excited"]], {}, class: "w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
    </div>

    <%# 本文 %>
    <div>
      <%= f.label :body, "本文", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_area :body, rows: 4, class: "w-full border rounded-md px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      <% if @entry.errors[:body].any? %>
        <p class="text-red-500 text-sm mt-1"><%= @entry.errors[:body].first %></p>
      <% end %>
    </div>

    <%# 写真 %>
    <div>
      <%= f.label :photo, "写真", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.file_field :photo, class: "w-full text-sm text-gray-600" %>
    </div>

    <%# 送信ボタン %>
    <div>
      <%= f.submit "投稿する", class: "w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 active:bg-blue-800 transition" %>
    </div>

  <% end %>
</div>
